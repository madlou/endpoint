version: "3.8"

services:
  webhook-monitor:
    build: .
    volumes:
      - webhook-logs:/app/logs
    restart: unless-stopped
    environment:
      - TZ=UTC

  nginx:
    image: nginx:alpine
    ports:
      - "8080:80"
    volumes:
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - webhook-monitor
    restart: unless-stopped

volumes:
  webhook-logs:
